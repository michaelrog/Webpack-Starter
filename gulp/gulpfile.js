/*
 * This main Gulp file loads a tasks.js file, which then loads a config module
 * and defines the Gulp tasks for this project.
 * The tasks module also exports an array of default tasks.
 */

const gulp = require('gulp');
const tasks = require('./gulp/gulp.tasks.js');

// gulp.task('default', gulp.series(...[]));

